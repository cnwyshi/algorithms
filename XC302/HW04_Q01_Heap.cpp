#include <bits/stdc++.h>
using namespace std;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, tmp;
    cin >> n;
    for(int i = 0; i < n; i++){
        cin >> tmp;
        if(tmp == 1){
            cin >> tmp;
        }
        if(tmp == 2){

        }
        if(tmp == 3){

        }
    }
}
